import Resolver, { Class } from "../interfaces/resolver";
/**
 * The default resolver.
 */
export declare class InstantiationResolver implements Resolver {
    add<T>(_type: Class<T>): void;
    resolve<T>(type: Class<T>): T;
}
